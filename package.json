{
  "name": "envprotect",
  "version": "1.0.0",
  "description": "Type-safe environment variable validation with Zod. Load, validate, and guard your env vars with full TypeScript inference, secrets masking, and .env.example generation.",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "bin": {
    "envprotect": "./dist/cli.js"
  },
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "test": "bun test",
    "prepublishOnly": "npm run build"
  },
  "keywords": [
    "env",
    "environment",
    "validation",
    "zod",
    "typescript",
    "dotenv",
    "config",
    "type-safe",
    "secrets",
    "masking",
    "envprotect"
  ],
  "license": "MIT",
  "peerDependencies": {
    "zod": "^3.22.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.5.0",
    "zod": "^3.23.0"
  }
}
